# Base URL of the deployed Cloudflare Worker METAR/TAF proxy (see proxy/).
# Example: https://drone-weather-metar-proxy.<your-subdomain>.workers.dev
VITE_METAR_PROXY_URL=

# Build stamp shown in the app footer. CI sets this to the git SHA.
VITE_APP_VERSION=dev
